import { StyleSheet } from 'react-native';

// StakingCell uses shared styles from shared.styles.ts
// This file is kept for consistency and potential cell-specific overrides
export const styles = StyleSheet.create({
  // Cell-specific styles can be added here if needed
});

