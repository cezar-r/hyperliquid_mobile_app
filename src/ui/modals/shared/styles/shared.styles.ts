import { StyleSheet } from 'react-native';
import { Color } from '../../../shared/styles/colors';

// Shared styles that can be reused across multiple modal components
export const sharedModalStyles = StyleSheet.create({
  // Add shared modal styles here as they are identified during refactoring
});

